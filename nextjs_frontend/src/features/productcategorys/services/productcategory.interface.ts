export interface ICreateCustomer {
  name: string;
  contract: string;
}

export interface ICustomer {
  id: string;
  name: string;
  contactInfo: string;
  createdAt: string;
  updatedAt: string;
}
